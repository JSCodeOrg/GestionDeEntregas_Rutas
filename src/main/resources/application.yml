spring:
  application:
    name: GestionDeEntregas_Rutas
  datasource:
    url: jdbc:postgresql://localhost:5432/GestionEntregasRutas
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

  sql:
    init:
      mode: never

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect